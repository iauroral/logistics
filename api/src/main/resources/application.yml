spring:
  session:
    store-type: hash_map
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:mysql://localhost:3306/logistics?characterEncoding=utf-8
    username: root
    password: root

server:
  port: 8000
